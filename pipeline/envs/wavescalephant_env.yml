name: wavescalephant_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python >= 3.7
  - pip >= 19.0.3
  - jinja2 >= 2.10
  - pygments >= 2.3.1
  - pygraphviz >= 1.3
  - pip:
    - snakemake >= 5.7.1
    # - neo >= 0.8.0
    - git+git://github.com/NeuralEnsemble/python-neo.git
    # - elephant >= 0.6.3
    - git+git://github.com/NeuralEnsemble/elephant.git
    - nixio >= 1.5.0b1
    - matplotlib
    - seaborn
    - networkx
