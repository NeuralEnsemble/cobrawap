name: wavescalephant_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python >= 3.7
  - pip >= 19.3.1
  - jinja2 >= 2.10.3
  - pygments >= 2.4.2
  - pygraphviz >= 1.5
  - snakemake >= 5.9.1
  - pip:
    # - elephant >= 0.6.3
    - git+git://github.com/NeuralEnsemble/elephant.git #(pip install from git repo branch)
    # - neo >= 0.8.0
    - git+https://github.com/G-Node/python-neo.git@fix/nixio-read-channel-names #(pip install from git repo branch)
    - nixio >= 1.5.0b4
    - pillow >= 6.2.1
    - matplotlib
    - seaborn
    - networkx
    - scikit-learn >= 0.21.3
    - shapely >=1.6.4.post2
